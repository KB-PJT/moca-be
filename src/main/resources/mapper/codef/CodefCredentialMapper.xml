<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.moca.mocabe.domain.codef.mapper.CodefCredentialMapper">

    <select id="existsByUserIdAndIssuerIdAndIdentityHash" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM codef_account_credentials
            WHERE user_id = #{userId}
              AND issuer_id = #{issuerId}
              AND credential_identity_hash = #{credentialIdentityHash}
        )
    </select>

    <insert id="insertAccountCredential">
        INSERT INTO codef_account_credentials (
            codef_account_credential_id,
            user_id,
            issuer_id,
            connected_id,
            account_id_enc,
            account_password_enc,
            birth_date_enc,
            pending_card_number_enc,
            pending_card_password_enc,
            credential_identity_hash,
            status,
            created_at,
            updated_at
        ) VALUES (
            #{codefAccountCredentialId},
            #{userId},
            #{issuerId},
            #{connectedId},
            #{accountIdEnc},
            #{accountPasswordEnc},
            #{birthDateEnc},
            #{pendingCardNumberEnc},
            #{pendingCardPasswordEnc},
            #{credentialIdentityHash},
            #{status},
            UTC_TIMESTAMP(6),
            UTC_TIMESTAMP(6)
        )
    </insert>

    <resultMap id="pendingCardDiscoveryTargetMap"
               type="com.moca.mocabe.domain.codef.model.PendingCardDiscoveryTarget">
        <constructor>
            <arg column="codef_account_credential_id" javaType="java.lang.String"/>
            <arg column="connected_id" javaType="java.lang.String"/>
            <arg column="institution_code" javaType="java.lang.String"/>
            <arg column="birth_date_enc" javaType="_byte[]"/>
            <arg column="requires_card_no" javaType="_boolean"/>
            <arg column="pending_card_number_enc" javaType="_byte[]"/>
            <arg column="pending_card_password_enc" javaType="_byte[]"/>
        </constructor>
    </resultMap>

    <!-- claim 트랜잭션 전용: 같은 연동 행을 잠가 동시 discover 요청이 같은 pending 값을 함께
         읽지 못하게 한다. CODEF 호출 전에만 쓰고, 이 SELECT를 감싼 트랜잭션은 곧바로 끝난다. -->
    <select id="findPendingDiscoveryTarget" resultMap="pendingCardDiscoveryTargetMap">
        SELECT c.codef_account_credential_id, c.connected_id, i.institution_code, c.birth_date_enc,
               i.requires_card_no, c.pending_card_number_enc, c.pending_card_password_enc
        FROM codef_account_credentials c
        INNER JOIN issuers i ON i.issuer_id = c.issuer_id
        WHERE c.codef_account_credential_id = #{linkId}
          AND c.user_id = #{userId}
        FOR UPDATE
    </select>

    <update id="clearPendingCardCredentials">
        UPDATE codef_account_credentials
        SET pending_card_number_enc = NULL,
            pending_card_password_enc = NULL,
            updated_at = UTC_TIMESTAMP(6)
        WHERE codef_account_credential_id = #{linkId}
          AND user_id = #{userId}
    </update>

    <!-- claim 이후 실패 시 복구 전용. claim 시점에 읽어둔 원래 값을 그대로 되돌린다. -->
    <update id="restorePendingCardCredentials">
        UPDATE codef_account_credentials
        SET pending_card_number_enc = #{pendingCardNumberEnc},
            pending_card_password_enc = #{pendingCardPasswordEnc},
            updated_at = UTC_TIMESTAMP(6)
        WHERE codef_account_credential_id = #{linkId}
          AND user_id = #{userId}
    </update>

    <select id="lockOwnedLink" resultType="string">
        SELECT codef_account_credential_id
        FROM codef_account_credentials
        WHERE codef_account_credential_id = #{linkId}
          AND user_id = #{userId}
        FOR UPDATE
    </select>

    <resultMap id="codefConnectionMap" type="com.moca.mocabe.domain.codef.model.CodefConnection">
        <constructor>
            <arg column="codef_account_credential_id" javaType="java.lang.String"/>
            <arg column="connected_id" javaType="java.lang.String"/>
            <arg column="institution_code" javaType="java.lang.String"/>
            <arg column="issuer_id" javaType="java.lang.String"/>
            <arg column="issuer_name" javaType="java.lang.String"/>
            <arg column="performance_lookback_months" javaType="java.lang.Integer"/>
            <arg column="birth_date_enc" javaType="_byte[]"/>
            <arg column="requires_card_no" javaType="_boolean"/>
            <arg column="requires_card_password" javaType="_boolean"/>
        </constructor>
    </resultMap>

    <!-- 회원 탈퇴 시 user_cards가 이 테이블을 참조하므로(fk_user_cards_codef_account_credential),
         user_cards를 먼저 정리한 뒤에 호출해야 한다. -->
    <delete id="deleteAccountCredentialsByUserId">
        DELETE FROM codef_account_credentials WHERE user_id = #{userId}
    </delete>

    <select id="findActiveConnectionsByUserId" resultMap="codefConnectionMap">
        SELECT c.codef_account_credential_id, c.connected_id, i.institution_code, c.issuer_id, i.issuer_name,
               i.performance_lookback_months, c.birth_date_enc, i.requires_card_no, i.requires_card_password
        FROM codef_account_credentials c
        INNER JOIN issuers i ON i.issuer_id = c.issuer_id
        WHERE c.user_id = #{userId}
          AND c.status = 'active'
        ORDER BY c.created_at ASC, c.codef_account_credential_id ASC
    </select>
</mapper>
